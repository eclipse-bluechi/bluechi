install_data(
  'hirte.conf',
  install_dir : get_option('sysconfdir')
)

install_data(
  'hirte-agent.conf',
  install_dir :  get_option('sysconfdir')
)

install_data(
  'hirte-default.conf',
  install_dir :  '/usr/share'
)
